<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.xw.mapper.UserMapper">

    <!-- 添加 -->
    <insert id="add" parameterType="com.xw.entity.User">
        INSERT INTO spring_boot_tb_user(username, password) VALUES (#{username}, #{password})
    </insert>

    <!-- 删除 -->
    <delete id="delete" parameterType="Long">
        DELETE FROM spring_boot_tb_user WHERE id = #{id}
    </delete>

    <!-- 更新 -->
    <update id="update" parameterType="com.xw.entity.User">
        UPDATE spring_boot_tb_user SET WHERE 1=1
        <if test="username != null">
            username = #{username},
        </if>
        <if test="password != null">
            password = #{password}
        </if>
        WHERE id = #{id}
    </update>

    <!-- 查询所有 -->
    <select id="findAll" resultType="com.xw.entity.User">
        select * from spring_boot_tb_user
    </select>

    <!-- 根据ID查询 -->
    <select id="findById" resultType="com.xw.entity.User" parameterType="Long">
        SELECT * FROM spring_boot_tb_user WHERE id = #{id}
    </select>

    <!-- 根据用户名查询 -->
    <select id="findByName" resultType="com.xw.entity.User" parameterType="String">
        SELECT * FROM spring_boot_tb_user WHERE username = #{username}
    </select>
</mapper>